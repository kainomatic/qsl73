PyYAML>=6.0
requests>=2.31
rapidfuzz>=3.0
# QR-Code-Decoding (Schritt 4b, ADR-0011, ADR-0017):
pymupdf>=1.24      # PDF → Rasterbild (alle Seiten)
Pillow>=10.0       # PIL-Bild-Konvertierung für zxingcpp
# Windows-only (PEP-508-Marker: werden auf Linux/CI automatisch ignoriert):
# Installationsname: zxing-cpp (Bindestrich); Importname im Code: zxingcpp (ohne Bindestrich)
zxing-cpp>=3.0 ; sys_platform == "win32"    # QR-Decoder; verifiziert mit cp312-Wheel (fixes #11)
pywin32>=306 ; sys_platform == "win32"       # DPAPI-Verschlüsselung (fixes #10)
# pyzbar wurde geprüft, scheitert an fehlender libzbar-64.dll auf Windows (Issue #7, ADR-0017).
